# 🤖 Telegram Бот для Учета Финансов

## 📋 Обзор

Telegram бот для учета личных финансов с использованием aiogram 3.10.0. Позволяет пользователям добавлять доходы и расходы, просматривать баланс и статистику через удобный интерфейс с inline-кнопками.

## 🏗️ Архитектура

### Основные компоненты

```
finance_bot/
├── main.py                    # Основной файл бота
├── src/
│   ├── __init__.py
│   └── message_parser.py      # Парсер сообщений транзакций
├── database/
│   ├── __init__.py
│   ├── database.py           # Модель и подключение к БД
│   └── transaction_repository.py # Репозиторий транзакций
├── config/
│   ├── __init__.py
│   └── config.py             # Конфигурация
└── .env                      # Переменные окружения
```

### Технологический стек

- **aiogram 3.10.0** - современная асинхронная библиотека для Telegram ботов
- **SQLAlchemy 2.0.31** - ORM для работы с базой данных
- **aiosqlite 0.20.0** - асинхронный драйвер SQLite
- **python-dotenv 1.0.0** - управление переменными окружения

## 🎯 Функциональность

### Основные команды

- `/start` - Приветствие и главное меню
- `/help` - Справка по использованию
- `/balance` - Показать текущий баланс
- `/stats` - Статистика по категориям (список транзакций)
- `/history` - История транзакций

### Пользовательский интерфейс

#### 🏠 Главное меню (inline-кнопки)
- **➕ Добавить** - Начать процесс добавления транзакции
- **💰 Баланс** - Просмотр текущего баланса
- **📊 Статистика** - Список транзакций для редактирования

#### 📝 Процесс добавления транзакции
1. **Выбор типа**: 📈 Доходы / 📉 Расходы
2. **Ввод данных**: `категория сумма` (например: `зарплата 100000`)
3. **Подтверждение**: ✅ Занести / ✏️ Изменить

#### ✏️ Редактирование транзакций
1. **Выбор транзакции** из списка
2. **Меню редактирования**:
   - ✏️ Категория
   - 💰 Сумма
   - 🔄 Тип (доход/расход)
   - 🗑️ Удалить

## 💾 Управление данными

### Временное хранилище

```python
user_temp_data = {
    user_id: {
        'type': 'income|expense',           # Для добавления
        'category': 'название категории',   # Временное хранение
        'amount': 1000.0,                  # Временное хранение

        # Для редактирования
        'edit_mode': True,
        'transaction_id': 123,
        'edit_field': 'category|amount|type'
    }
}
```

### Форматы сообщений

#### Парсер транзакций (`message_parser.py`)
```python
# Поддерживаемые форматы:
"доходы зарплата 100000"
"доход зарплата 50000"
"расходы продукты 2500"
"расход еда 1500"
```

#### Callback данные
```python
# Добавление транзакции
"add_transaction"
"income" / "expense"
"confirm_transaction"
"change_transaction"

# Редактирование
"edit_transaction_123"
"edit_category_123"
"edit_amount_123"
"edit_type_123"
"delete_transaction_123"
"show_transactions"
"back_to_menu"
```

## 🔧 Обработчики (Handlers)

### Message Handlers
- `send_welcome()` - Команда /start
- `send_help()` - Команда /help
- `show_balance()` - Команда /balance
- `show_stats()` - Команда /stats
- `show_history()` - Команда /history
- `handle_category_amount()` - Обработка ввода данных
- `handle_transaction()` - Обработка текстовых транзакций (legacy)

### Callback Query Handlers
- `callback_add_transaction()` - Кнопка "Добавить"
- `callback_show_balance()` - Кнопка "Баланс"
- `callback_show_stats()` - Кнопка "Статистика"
- `callback_transaction_type()` - Выбор типа транзакции
- `callback_confirm_transaction()` - Подтверждение добавления
- `callback_change_transaction()` - Изменение транзакции
- `callback_edit_transaction()` - Выбор транзакции для редактирования
- `callback_edit_field()` - Редактирование полей
- `callback_back_to_menu()` - Возврат в главное меню
- `callback_back_to_transactions()` - Возврат к списку

## 🎨 UI Компоненты

### Inline Keyboard Functions
```python
def get_welcome_keyboard() -> InlineKeyboardMarkup
def get_transaction_type_keyboard() -> InlineKeyboardMarkup
def get_confirmation_keyboard() -> InlineKeyboardMarkup
def get_transaction_list_keyboard(transactions: List) -> InlineKeyboardMarkup
def get_edit_transaction_keyboard(transaction_id: int) -> InlineKeyboardMarkup
```

### Reply Keyboard Functions
```python
def get_main_keyboard() -> ReplyKeyboardMarkup
```

## ⚙️ Конфигурация

### Переменные окружения (.env)
```env
BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN
```

### Конфигурация бота
```python
# main.py
BOT_TOKEN = os.getenv("BOT_TOKEN")

# Настройки
parse_mode=ParseMode.HTML
default=DefaultBotProperties(parse_mode=ParseMode.HTML)
```

## 🔄 Обработка ошибок

### Типы ошибок и их обработка

1. **Отсутствие токена**:
   ```python
   if not BOT_TOKEN:
       raise ValueError("BOT_TOKEN не найден!")
   ```

2. **Неправильный формат сообщения**:
   ```python
   if len(parts) < 2:
       await message.answer("❌ Неверный формат! Введите: `категория сумма`")
   ```

3. **Ошибки базы данных**:
   ```python
   try:
       # Операции с БД
   except Exception as e:
       logger.error(f"Error: {e}")
       await message.answer("❌ Произошла ошибка")
   finally:
       await session.close()
   ```

## 🚀 Запуск и развертывание

### Разработка
```bash
# Активация виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# Установка зависимостей
pip install -r requirements.txt

# Настройка токена
cp .env.example .env
# Редактировать .env файл

# Запуск бота
python main.py
```

### Production

#### Systemd Service (Linux)
```ini
[Unit]
Description=Finance Telegram Bot
After=network.target

[Service]
Type=simple
User=financebot
WorkingDirectory=/path/to/finance_bot
ExecStart=/path/to/venv/bin/python main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

#### Docker
```dockerfile
FROM python:3.11-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .
CMD ["python", "main.py"]
```

## 🔍 Мониторинг и логирование

### Настройка логирования
```python
import logging

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

# Логирование важных событий
logger.info(f"Transaction added: {transaction_data}")
logger.error(f"Database error: {error}")
```

### Метрики для мониторинга
- Количество активных пользователей
- Количество транзакций в день
- Время ответа бота
- Ошибки базы данных

## 🔐 Безопасность

### Защита данных
1. **Токен бота** - хранится в .env файле
2. **Валидация ввода** - проверка формата сообщений
3. **Изоляция пользователей** - каждый пользователь работает только со своими данными
4. **SQL Injection** - защита через SQLAlchemy ORM

### Рекомендации по безопасности
- Регулярное обновление зависимостей
- Использование HTTPS для webhook (если используется)
- Ограничение доступа к базе данных
- Резервное копирование данных

## 📈 Масштабирование

### Вертикальное масштабирование
- Увеличение памяти/CPU сервера
- Оптимизация запросов к БД
- Кэширование данных

### Горизонтальное масштабирование
- Нагрузочный балансер
- Несколько экземпляров бота
- Репликация базы данных

## 💡 Идеи для улучшения

### Функциональные улучшения
- [ ] Категории по умолчанию
- [ ] Ежемесячные/годовые отчеты
- [ ] Экспорт данных (CSV/Excel)
- [ ] Напоминания о регулярных транзакциях
- [ ] Мультивалютность
- [ ] Графики и аналитика в боте
- [ ] Бюджеты и лимиты категорий
- [ ] Инвестиции и сбережения

### Технические улучшения
- [ ] Redis для кэширования
- [ ] PostgreSQL вместо SQLite
- [ ] API для внешних приложений
- [ ] Интеграция с банками
- [ ] WebSocket для реального времени
- [ ] Машинное обучение для прогнозирования

### UI/UX улучшения
- [ ] Адаптивные inline-кнопки
- [ ] Быстрые команды (секстые)
- [ ] Анимированные интерфейсы
- [ ] Темная/светлая тема
- [ ] Локализация (i18n)
- [ ] Голосовые команды

## 🔗 Связанные компоненты

- [[База Данных]] - Модель данных и SQLAlchemy репозитории
- [[Streamlit Дашборд]] - Визуализация финансовых данных
- [[API]] - Возможное REST API для интеграций

## 📚 Полезные ресурсы

- [aiogram Documentation](https://docs.aiogram.dev/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [SQLAlchemy Documentation](https://docs.sqlalchemy.org/)
- [Python Asyncio](https://docs.python.org/3/library/asyncio.html)

---

**Теги:** `telegram-bot` `aiogram` `python` `async` `finance` `sqlite` `user-interface` `inline-buttons`